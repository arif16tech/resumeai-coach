# 🚀 ResumeAI Coach — Resume Analysis & Interview Prep

A full-stack AI career platform that analyzes resumes, generates personalized interview questions, and provides AI-powered interview preparation using MERN and Gemini AI.

---

## 🛠️ Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend | React 19 + Vite + Tailwind CSS v4 + Framer Motion |
| Components & UI | Lucide React (Icons), Recharts (Data Visualization) |
| Backend | Node.js + Express (ES Modules) |
| Database | MongoDB + Mongoose |
| Auth & Security | JWT (httpOnly cookies) + bcrypt + Google OAuth 2.0 + Helmet + Express Rate Limit |
| File Upload | Multer (Memory Storage) + Cloudinary + Streamifier |
| AI | Google Gemini API (`gemini-2.0-flash`) |
| PDF Parsing | `pdf-parse` |

---

## 📁 Project Structure

```text
resumeai-coach/
├── backend/
│   ├── src/
│   │   ├── config/          # DB, Cloudinary configs
│   │   ├── controllers/     # Auth, Resume, Interview, Chat, Dashboard
│   │   ├── middleware/      # Auth JWT, Error handler, Upload (Multer), Rate Limiters
│   │   ├── models/          # User, Resume, Interview (Mongoose)
│   │   ├── routes/          # Express API routes
│   │   ├── services/        # Gemini AI, PDF parsing, Cloudinary streaming
│   │   └── server.js        # Entry point (Express + Helmet + API limiters)
│   ├── .env
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── api/             # Axios interceptors & instances
    │   ├── components/      # Layout, AIAssistant, ScoreRing, etc.
    │   ├── context/         # AuthContext
    │   ├── hooks/           # useVoiceInput
    │   ├── pages/
    │   │   ├── auth/        # Login, Register
    │   │   ├── dashboard/   # Dashboard with stats and Recharts
    │   │   ├── resume/      # Upload & Detail pages
    │   │   └── interview/   # Setup, Session, Result, History
    │   ├── App.jsx          # Routes + Auth guards
    │   └── main.jsx
    ├── .env
    └── package.json
```

---

## ⚙️ Prerequisites

- Node.js v18+
- MongoDB (local or Atlas)
- Cloudinary account
- Google Cloud OAuth credentials
- Google Gemini API key

---

## 🌍 Live Demo

https://resumeai-coach.vercel.app/

---

## 🚀 Quick Start

### 1. Clone the project

```bash
git clone https://github.com/arif16tech/resumeai-coach.git
cd resumeai-coach
```

### 2. Set up the Backend

```bash
cd backend
npm install
```
Create a `.env` file in the `backend` folder:

```env
NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://localhost:27017/ai-resume-coach
JWT_SECRET=your_super_secret_key_min_32_chars
CLIENT_URL=http://localhost:5173

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your_google_client_id

# Gemini
GEMINI_API_KEY=your_gemini_api_key
```

Start backend:
```bash
npm run dev
```

### 3. Set up the Frontend

```bash
cd ../frontend
npm install
```

Create a `.env` file in the `frontend` folder:

```env
VITE_API_URL=http://localhost:5000/api
VITE_GOOGLE_CLIENT_ID=your_google_client_id
```

Start the frontend:
```bash
npm run dev
```

---

## ✨ Features

### 🔐 Authentication & Security
- Email/password registration & login
- **Google OAuth 2.0** — one-click sign-in via Google account
- **OTP-based verification** — email one-time password flow for account confirmation
- JWT stored securely in `httpOnly` cookies
- Password hashing with bcrypt
- **Helmet.js** for securing Express HTTP headers
- **Express Rate Limiting** to prevent abuse (user-based API limits for AI endpoints)

### 📄 Resume Module
- Upload PDF resume (up to 10MB), parsed entirely in-memory (`multer.memoryStorage`)
- Stream direct to Cloudinary without saving files to disk
- Gemini AI analysis:
  - **ATS Score (0–100):** Measures parsability and keyword matches
  - **Content Quality Score (0–100):** Evaluates writing impact, grammar, and action verbs
  - Strengths, Weaknesses, Suggestions
  - Section-by-section feedback (Contact, Summary, Experience, Education, Skills)
  - Keyword gap analysis vs job description
- Re-analyze with updated job description
- Daily user limits applied to prevent AI abuse (Max 2 analyses per day)

### 🎤 Interview Module
Three modes:
- **Resume-based**: Questions generated directly from your resume's context
- **Technical**: Role-specific coding/system-design questions
- **HR/Behavioral**: Behavioral, culture-fit, and situational questions

Features:
- Choose difficulty (Easy / Medium / Hard) and total questions
- Voice input integration (Web Speech API)
- Real-time AI evaluation for each answer providing specific, actionable feedback and scores
- **Suggested Answers:** A concise (<100 words) suggested answer generated by AI, revealed only on the final Results page
- Final score and comprehensive summary of strong/weak areas
- Daily user limits applied (Max 3 interviews per day)

### 💬 Global AI Assistant
- Persistent chat modal available throughout the app
- Context-aware (understands the current interview question or resume context)
- Career coaching, answer improvement, concept explanation

### 📊 Dashboard
- Visualize data with **Recharts**
- Track total resumes, interviews, and completed sessions
- Monitor average interview scores and top weak areas
- View recent history

---

## 🌐 API Endpoints

### Auth
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/auth/register` | Register with email/password |
| POST | `/api/auth/login` | Login with email/password |
| POST | `/api/auth/logout` | Logout |
| GET | `/api/auth/me` | Get current authenticated user |
| GET | `/api/auth/google` | Initiate Google OAuth 2.0 login |
| GET | `/api/auth/google/callback` | Google OAuth callback handler |

### Resume
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/resume/upload` | Upload + analyze resume (Rate Limited) |
| GET | `/api/resume` | Get all user resumes |
| GET | `/api/resume/:id` | Get single resume |
| DELETE | `/api/resume/:id` | Delete resume |
| POST | `/api/resume/:id/reanalyze` | Re-analyze with new JD (Rate Limited) |

### Interview
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/interview/start` | Start new interview session (Rate Limited) |
| POST | `/api/interview/:id/answer` | Submit answer for AI feedback |
| GET | `/api/interview` | Get all interviews |
| GET | `/api/interview/:id` | Get interview details (includes suggested answers) |
| GET | `/api/interview/:id/current` | Get current active question |

---

## 🛡️ Stability & Security Improvements

This project has been hardened for MVP-level stability:
- **Database Indexing:** Compound indexes (`{ user: 1, createdAt: -1 }`) on `Resume` and `Interview` models to prevent slow collection scans.
- **Rate Limiting:** Specific endpoint limiters:
  - Auth routes (`/login`, `/register`): 5 requests / 15 minutes.
  - AI endpoints (`/api/interview`, `/api/resume/upload`): strict daily & per-minute limits to protect Gemini API quota.
- **Robust Error Handling:** Safe JSON parsing for AI responses using `try/catch` and custom `AppError` throws to gracefully handle malformed AI text.
- **Resource Cleanup:** Automated Cloudinary file deletion if PDF text extraction fails, preventing orphaned file storage costs.
- **Secure Configuration:** Configured for `NODE_ENV=production` environments to automatically secure cookies and enable proper optimizations.

---

## 📝 Notes & Best Practices

- **Security:** Helmet is used for header security. Production environments must strictly set the `CLIENT_URL` in `.env` for accurate CORS policy enforcement.
- **File Uploads:** Files are processed dynamically in-memory and streamed to Cloudinary using `streamifier` to bypass disk I/O bottlenecks.
- **AI Integration:** The system uses `gemini-2.0-flash` and native JSON generation mode (`responseMimeType: 'application/json'`) ensuring strict, parseable AI responses without regex hacks.
- **Voice Input:** Voice input utilizes the browser's native Web Speech API (Chrome/Edge recommended).